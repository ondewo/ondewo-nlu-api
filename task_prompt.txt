Add missing descriptions to messages, message fields, nested messages, and enum values in all proto files in ondewo/nlu, starting from from operation_metadata.proto

Requirements:
1. Add field descriptions for all message fields that are missing them (format: // Description above field)
2. Add message-level descriptions for all nested messages (format: // Description above message declaration)
3. Add descriptions for all enum values that are missing them (format: // Description above enum value)
4. Create/update added_descriptions_list.txt listing all changes organized by: MESSAGES, ENUMS, NESTED MESSAGES

Do NOT:
- Modify RPC method descriptions (only messages/enums)
- Change existing descriptions
- Modify field types, names, or structure

Follow existing comment style and patterns. Ensure no linter errors. Verify all descriptions are added before completing.

